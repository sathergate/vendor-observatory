{
  "buildCommand": "rm -rf apps/web/.next && pnpm --filter @obs/shared build && pnpm --filter web build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install",
  "framework": "nextjs"
}
